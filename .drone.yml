workspace:
  base: /test
  path: openeuropa-site

services:
  web:
    image: fpfis/httpd-php-dev:${PHP_VERSION=7.1}
  mysql:
    image: percona/percona-server:5.6
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=yes
  selenium:
    image: selenium/standalone-chrome:3.11
    environment:
      - DISPLAY=:99
      - SE_OPTS=-debug

pipeline:
  # Download all dependencies.
  composer-create-project:
    group: pre-prepare
    image: fpfis/httpd-php-dev:${PHP_VERSION=7.1}
    volumes:
      - /cache:/cache
    commands:
      - composer create-project --repository-url=packages.json --ansi --no-progress --no-interaction openeuropa/drupal-site-template drupal-site-test

  # Initialize git.
  git-init:
    image: fpfis/httpd-php-dev:7.1
    commands:
      - cd drupal-site-test
      - git init

  # Install the drupal site.
  site-install:
    image: fpfis/httpd-php-dev:7.1
    commands:
      - cd drupal-site-test
      - ./vendor/bin/run drupal-dev:site-setup
      - ./vendor/bin/run drupal:site-install

  # Run grumphp tests.
  test-grumphp:
    group: test
    image: fpfis/httpd-php-dev:${PHP_VERSION=7.1}
    commands:
      - cd drupal-site-test
      - ./vendor/bin/grumphp run

  # Run behat tests.
  test-behat:
    group: test
    image: fpfis/httpd-php-dev:${PHP_VERSION=7.1}
    commands:
      - cd drupal-site-test
      # Reset permission since installation runs as root. @todo Fix this.
      - ./vendor/bin/drush -y config-set system.performance css.preprocess 0
      - ./vendor/bin/drush -y config-set system.performance js.preprocess 0
      - ./vendor/bin/behat --strict

matrix:
  PHP_VERSION:
    - 7.1
    - 7.2
